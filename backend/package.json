{
  "name": "safarai-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SafarAI backend services — AI (Groq), database (Supabase), data layer, and dev/audit scripts.",
  "scripts": {
    "images:fetch": "node scripts/fetch-images.mjs",
    "images:fetch:dry": "node scripts/fetch-images.mjs --dry",
    "audit": "node scripts/audit.mjs",
    "audit:local": "node scripts/audit.mjs http://localhost:5173",
    "start": "node -e \"import('./services/index.js').then(m => console.log('Backend services loaded OK:', Object.keys(m)))\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.2",
    "dotenv": "^16.4.5",
    "groq-sdk": "^1.1.1"
  },
  "devDependencies": {
    "axe-core": "^4.13.0",
    "jsdom": "^30.0.1",
    "puppeteer-core": "^25.8.0"
  }
}
